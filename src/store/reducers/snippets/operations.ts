import * as actions from './actions';

const fetchSnippets = actions.fetchSnippets;
const addNewSnippet = actions.addNewSnippet;
const fetchSnippet = actions.fetchSnippet;
const clearCodeSnippet = actions.clearCodeSnippet;
const updateSnippet = actions.updateSnippet;

export { fetchSnippets, addNewSnippet, fetchSnippet, clearCodeSnippet, updateSnippet };
